import { SearchtitlePipe } from './searchtitle.pipe';

describe('SearchtitlePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchtitlePipe();
    expect(pipe).toBeTruthy();
  });
});
